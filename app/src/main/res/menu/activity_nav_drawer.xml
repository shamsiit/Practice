<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_games"
            android:icon="@drawable/ic_menu_send"
            android:title="Browse Games" />
        <item
            android:id="@+id/nav_prize"
            android:icon="@drawable/ic_menu_send"
            android:title="Browse Prize" />
        <item
            android:id="@+id/nav_conn"
            android:icon="@drawable/ic_menu_send"
            android:title="Browse Conn" />
        <item
            android:id="@+id/nav_position"
            android:icon="@drawable/ic_menu_send"
            android:title="My Position" />
        <item
            android:id="@+id/nav_leader"
            android:icon="@drawable/ic_menu_send"
            android:title="Leader Board" />
    </group>

    <item android:title="Sub">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_send"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
